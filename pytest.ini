# pytest.ini
[pytest]
markers =
    integration: marks tests as integration tests (require API key, network access)

filterwarnings =
    # Ignore the specific DeprecationWarning from google._upb related to PyType_Spec
    ignore:Type google\._upb\._message\.(Message|Scalar)MapContainer uses PyType_Spec:DeprecationWarning